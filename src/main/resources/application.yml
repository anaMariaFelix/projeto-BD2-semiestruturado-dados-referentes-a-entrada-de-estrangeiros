spring:
  datasource:
    url: jdbc:mysql://localhost:3306/semiestruturadoestrangeiro?serverTimezone=UTC
    username: root
    password: root

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update 
    properties:
      hibernate.format_sql: true